# Try to force Gradle to use an older JDK for its daemon
# This is a guess, actual paths might differ in the environment
org.gradle.java.home=/usr/lib/jvm/java-17-openjdk-amd64

kotlin.code.style=official
kotlin.incremental.js=true
kotlin.incremental.js.ir=false
kotlin.native.cacheKind=none
# kotlin.native.home=
# kotlin.incremental.native=true
# kotlin.incremental.multiplatform=true

# Kotlin Multiplatform
# org.jetbrains.kotlin.native.home=

# Disable memory daemon for building in CI because it uses a lot of memory
kotlin.daemon.jvmargs=-Xmx1g
# kotlin.native.disableCompilerDaemon=true

# Enable IR backend for JS
kotlin.js.compiler=ir
# kotlin.js.compiler=legacy

# Other properties
# org.gradle.caching=true
# org.gradle.parallel=true
# systemProp.kotlin.incremental=true
# systemProp.kotlin.parallel.tasks.in.project=true
# systemProp.kotlin.compiler.execution.strategy=daemon
# systemProp.kotlin.daemon.jvm.options=Xmx2G
# Ensure tests are always run
# tasks.withType(Test).configureEach {
#     outputs.upToDateWhen { false }
# }
